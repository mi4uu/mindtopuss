vcs:
  manager: 'git'
  defaultBranch: 'master'
  provider: 'github'
  syncHooks: true
  hooks:
    pre-commit: 
    - 'bunx moon run :lint :format --affected --status=staged'

projects:
  - "."
  - "packages/*"